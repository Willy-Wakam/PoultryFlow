# Infrastructure

This directory will own local and deployment infrastructure definitions for PoultryFlow.

US-001 intentionally contains documentation only. US-002 will introduce a Docker Compose-based local development stack with PostgreSQL and Keycloak service definitions. S3-compatible object storage and deployment infrastructure may be added in later stories when required.

No infrastructure service is required to run the current backend or frontend skeleton.
